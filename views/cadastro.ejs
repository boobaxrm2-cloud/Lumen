<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <title>Criar conta — Lumen</title>
  <%- include('partials/head') %>
</head>
<body>
  <div class="auth-tela">
    <div class="ficha-auth">
      <h1><svg class="icone" viewBox="0 0 24 24"><circle cx="12" cy="12" r="4.2"/><path d="M12 2v2.4M12 19.6V22M4.9 4.9l1.7 1.7M17.4 17.4l1.7 1.7M2 12h2.4M19.6 12H22M4.9 19.1l1.7-1.7M17.4 6.6l1.7-1.7"/></svg> Nova ficha</h1>
      <p class="subtitulo">Crie sua conta de pesquisador para começar.</p>

      <% if (erro) { %>
        <div class="erro"><%= erro %></div>
      <% } %>

      <form method="POST" action="/cadastro">
        <div class="campo">
          <label for="name">Nome</label>
          <input type="text" id="name" name="name" value="<%= valores.name %>" required autofocus />
        </div>
        <div class="campo">
          <label for="email">Email</label>
          <input type="email" id="email" name="email" value="<%= valores.email %>" required />
        </div>
        <div class="campo">
          <label for="password">Senha</label>
          <input type="password" id="password" name="password" minlength="6" required />
        </div>
        <button class="botao acao-larga" type="submit">Criar conta</button>
      </form>

      <p class="rodape-ficha">Já tem conta? <a href="/login">Entrar</a></p>
    </div>
  </div>
</body>
</html>
